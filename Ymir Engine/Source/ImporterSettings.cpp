#include "ImporterSettings.h"

uint ImporterSettings::Save(const Settings& ourSettings, char** fileBuffer)
{
	return uint();
}

void ImporterSettings::Load(const char* fileBuffer, Settings& ourSettings)
{

}
